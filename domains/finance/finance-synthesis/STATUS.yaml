# Phase manifest — read by a new session to resume where the last one left off.
# phase states: todo | partial | done
domain: finance
leaf: finance-synthesis
depth: standard          # scout | standard | exhaustive (see _shared/sessions/depth-profiles.md)
phases:
  meta_research: done
  a_survey: partial         # -> sources
  b_ingest: partial         # -> documents + FTS
  c_extract: partial        # -> entity tables
  d_gold: partial           # -> verified facts (the irreducible layer)
  e_relationships: partial  # -> typed graph
updated: 2026-06-26
