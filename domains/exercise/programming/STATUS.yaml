# Phase manifest — read by a new session to resume where the last one left off.
# phase states: todo | partial | done
domain: exercise
leaf: programming
depth: exhaustive        # scout | standard | exhaustive (see _shared/sessions/depth-profiles.md)
phases:
  meta_research: done
  a_survey: done         # -> sources (69 in registry; 32 T0 metas + 13 T1 RCTs)
  b_ingest: done         # -> 53 docs (all fetchable tiers ingested; hard-paywalled = cite-only, OA mirrors used)
  c_extract: done        # -> 103 training_variables loaded (53 meta-grade)
  d_gold: done           # -> claims verified (programming share of 22; adversarial)
  e_relationships: done  # -> relationships graph projected (2291 typed edges domain-wide)
updated: 2026-06-25
