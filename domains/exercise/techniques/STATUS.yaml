# Phase manifest — read by a new session to resume where the last one left off.
# phase states: todo | partial | done
domain: exercise
leaf: techniques
depth: exhaustive        # scout | standard | exhaustive (see _shared/sessions/depth-profiles.md)
phases:
  meta_research: done
  a_survey: done         # -> sources (42 in registry; 12 T0 + 17 T1)
  b_ingest: done         # -> 28 docs (all fetchable tiers ingested; hard-paywalled = cite-only, OA mirrors used)
  c_extract: done        # -> 13 set_structures loaded
  d_gold: done           # -> claims verified (techniques share of 22; adversarial)
  e_relationships: done  # -> relationships graph projected (2291 typed edges domain-wide)
updated: 2026-06-25
