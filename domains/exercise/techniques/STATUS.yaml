# Phase manifest — read by a new session to resume where the last one left off.
# phase states: todo | partial | done
domain: exercise
leaf: techniques
depth: exhaustive        # scout | standard | exhaustive (see _shared/sessions/depth-profiles.md)
phases:
  meta_research: done
  a_survey: done         # -> sources (42 in registry; 12 T0 + 17 T1)
  b_ingest: partial      # -> 25/39 T0-T2 ingested (14 paywalled-blocked, mirrors used); T3 deferred
  c_extract: todo        # -> set_structures
  d_gold: todo           # -> verified claims (the irreducible layer)
  e_relationships: todo  # -> typed graph
updated: 2026-06-25
