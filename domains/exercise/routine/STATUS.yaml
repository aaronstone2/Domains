# Phase manifest — read by a new session to resume where the last one left off.
# phase states: todo | partial | done
domain: exercise
leaf: routine
depth: standard          # scout | standard | exhaustive (see _shared/sessions/depth-profiles.md)
phases:
  meta_research: done
  a_survey: done         # -> n/a (synthesis leaf; consumes the corpus)
  b_ingest: done         # -> n/a
  c_extract: done        # -> gyms + gym_equipment + gym_plates (typed load algebra) + gym_loadable view; 290 Revolution profile
  d_gold: done           # -> ROUTINE.md generated: balanced PPL x2 + dynamic mesocycle, real loads, cited, constraint-routed
  e_relationships: done  # -> exercises x gym load-algebra x training_variables x claims x constraints joined into the routine
updated: 2026-06-26
