# Phase manifest — read by a new session to resume where the last one left off.
# phase states: todo | partial | done
domain: exercise
leaf: anatomy
depth: exhaustive        # scout | standard | exhaustive (see _shared/sessions/depth-profiles.md)
phases:
  meta_research: done
  a_survey: done         # -> sources (35 in registry)
  b_ingest: done         # -> 29 docs (all fetchable tiers ingested; hard-paywalled = cite-only, OA mirrors used)
  c_extract: done        # -> muscles 58 + concepts (81 domain-wide glossary) loaded
  d_gold: done           # -> contested-anatomy claims verified (lat/calf/incline/hip-thrust/RF)
  e_relationships: done  # -> relationships graph projected (2291 typed edges domain-wide)
updated: 2026-06-26
