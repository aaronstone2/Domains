# notes/reports/ — synthesized writeups

Drop RCA writeups, Devin session summaries, anything I (the AI) write for
your review. Filename: `YYYY-MM-DD-HHMM-shortname.md` so they sort by
recency. Everything in this directory is gitignored except this README.

```bash
# Find latest:
ls -t notes/reports/ | head -5

# After AI writes a writeup, you can review + then promote anything worth
# keeping into the repo (e.g. cluely/ for talk-tracks, practice/ for new
# scenarios).
```
